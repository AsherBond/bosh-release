standby_mode = 'on'
primary_conninfo = 'host=<%= properties.dns.address %> port=<%= properties.dns.database.port || "5432" %> user=<%= properties.dns.replication.user %> password=<%= properties.dns.replication.password %>'
